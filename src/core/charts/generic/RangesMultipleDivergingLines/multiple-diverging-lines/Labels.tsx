import React from 'react'
import { ComputedDatum } from './types'

interface LabelsProps {
    data: ComputedDatum[]
    itemHeight: number
    width: number
}

export const Labels = ({ data, itemHeight, width }: LabelsProps) => {
    return null
}